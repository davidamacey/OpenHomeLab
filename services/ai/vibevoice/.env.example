# ===========================================================================
# VibeVoice — Environment Variables
# Copy to .env and fill in real values. .env is gitignored.
# ===========================================================================

VIBEVOICE_PORT=8300
GPU_DEVICE_ID=0    # GPU for voice synthesis
NAS_PATH=/mnt/nas  # Models expected at ${NAS_PATH}/models/vibevoice/
TZ=America/New_York
